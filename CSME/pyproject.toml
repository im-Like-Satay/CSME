[project]
name = "csme"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    # core deps
    "fastapi>=0.135.3",
    "psycopg[binary,pool]>=3.3.3",
    "pymemcache>=4.0.0",

    # side deps
    "pydantic>=2.12.5",
]
